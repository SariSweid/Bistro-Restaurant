package enums;

/**
 * Enum representing the days of the week specifically for restaurant settings.
 * Used to configure and reference weekly opening hours in the restaurant system.
 */
public enum RestaurantSettingsDays {
    Sunday,
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday
}
